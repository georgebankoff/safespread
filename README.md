# Archived SafeSpread workspace

This branch preserves historical recovery work, but its application is not the
current SafeSpread product and must not be deployed. Build the current app only
from the latest `origin/main` in `SafeSpreadVIO/`.

This archived app uses a deliberately different display name and iOS bundle
identifier so it cannot overwrite the current app on a phone.
